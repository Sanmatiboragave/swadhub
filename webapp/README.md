# Food Delivery Webapp (demo)

Scaffolded Vite + React + Tailwind demo with components for a modern food delivery homepage.

Run locally:

```bash
npm install
npm run dev
```

Open http://localhost:5173

Files of interest: `src/components` and `src/pages/Home.jsx`.
